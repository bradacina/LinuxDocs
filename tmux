/etc/tmux.conf -> config file
/usr/share/tmux -> config

ctrl-b -> the default trigger key

ctrl-b l -> move to previously selected window
ctrl-b w -> list all windows
ctrl-b <window #> -> go to that window
ctrl-b q -> show pane numbers
ctrl-b f -> find window by name
ctrl-b [ -> start scrolling

ctrl-b d -> detach from session

tmux attach -> attach to a running session
ctrl-b -> rename window
ctrl-b ? -> list key bindings
ctrl-b % -> splits into 2 vertical panes
ctrl-b " -> split into 2 horizontal panes
ctrl-b c -> create new window
ctrl-b x -> close window
ctrl-b n -> next window
ctrl-b p -> prev window
ctrl-b left/right -> move to the left/right pane
ctrl-b pgup/pgdown -> scroll buffer up/down
